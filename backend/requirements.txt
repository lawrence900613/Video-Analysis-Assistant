fastapi==0.115.6
uvicorn[standard]==0.34.0
# yt-dlp >= 2026.6.9 ships the reworked Instagram extractor that relies on
# browser impersonation for anonymous (cookie-less) access. Requires Python >= 3.10.
yt-dlp>=2026.6.9
# curl_cffi provides the browser TLS/JA3 fingerprint impersonation that Instagram
# now requires for anonymous requests. Without it Instagram returns an empty
# media response. See https://github.com/yt-dlp/yt-dlp/issues/17074
curl_cffi>=0.7
openai==1.59.6
python-dotenv==1.0.1
pydantic==2.10.4
